## Simulation slow subscriber
<img width="1582" height="961" alt="Screenshot 2026-05-12 at 19 04 15" src="https://github.com/user-attachments/assets/1473e54b-9b12-44d6-8c95-4f91f6a2069b" />

The screenshot displays the slow subscriber simulation, which highlights a clear mismatch between the data production and consumption rates in your event-driven system. The message rates chart shows that the Publish rate peaks quickly as the publisher sends its data at full speed, while the deliver rate is throttled by the artificial delay we have introduced in the subscriber's code. This behavior confirms that the publisher can operate independently of the subscriber's performance, allowing the system to remain responsive even when the final processing stage is significantly slower than the initial data entry.

In this specific screenshot, the total number of queued messages peaks at 1, as shown by the red line in the top chart hitting the 1.0 mark on the Y-axis. This number represents the "backlog" of messages that have successfully reached the AWS broker but are currently waiting for the subscriber to finish its previous task. The count is "as such" because RabbitMQ is performing Load Leveling; it acts as a protective buffer that holds messages in a "Ready" state until the slow subscriber is capable of processing them. This prevents the subscriber from being overwhelmed by a burst of traffic and ensures that no data is lost during the transition from the fast publisher to the throttled consumer.

## Running at least three subscribers

<img width="1582" height="961" alt="Screenshot 2026-05-12 at 19 18 31" src="https://github.com/user-attachments/assets/5724a325-4e77-4520-aa4f-3c8efec24606" />
<img width="1470" height="956" alt="Screenshot 2026-05-12 at 19 17 19" src="https://github.com/user-attachments/assets/b993e378-27ca-4724-90f3-695f238fe0f3" />

These screenshots capture the transition from a single-point system to a Competing Consumers pattern, which is a core principle of scaling distributed applications. The development environment shows three active terminal instances all running the subscriber program simultaneously. The source code in the background highlights the intentional 'thread::sleep' delay, but the logs in the foreground reveal that the workload is being shared, rather than one subscriber handling everything slowly, RabbitMQ is distributing the messages across all three instances. This "Round Robin" behavior ensures that each message is processed exactly once by whichever consumer is currently available, significantly increasing the total throughput of the system.

The second screenshot provides the administrative proof of this multi-consumer setup through the RabbitMQ Management Dashboard. Under the "Global counts" section, we can see that the number of Connections, Channels, and Consumers has all increased to 3, directly corresponding to the three terminals that are running. The message rate chart displays a more complex overlapping of spikes compared to previous runs, reflecting how the broker is coordinating with multiple clients at once. By having three "slow" subscribers instead of one, the system can now process up to three messages in parallel, effectively cutting the total processing time for a large batch of events by two-thirds and proving the power of horizontal scaling in a cloud environment.
